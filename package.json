{
  "name": "textlint-rule-max-kanji-continuous-len",
  "repository": {
    "type": "git",
    "url": "https://github.com/textlint-ja/textlint-rule-max-kanji-continuous-len.git"
  },
  "author": "azu",
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/textlint-ja/textlint-rule-max-kanji-continuous-len",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/textlint-ja/textlint-rule-max-kanji-continuous-len/issues"
  },
  "files": [
    "src/",
    "lib/"
  ],
  "version": "1.1.1",
  "description": "textlint rule that limit max continuous length of kanji(漢字).",
  "main": "lib/textlint-rule-max-kanji-continuous-len.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha test/",
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build"
  },
  "keywords": [
    "textlint",
    "textlintrule"
  ],
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-jsdoc-to-assert": "^2.0.1",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.8.0",
    "mocha": "^2.4.5",
    "power-assert": "^1.3.1",
    "textlint-tester": "^2.0.0"
  },
  "dependencies": {
    "match-index": "^1.0.1",
    "textlint-rule-helper": "^2.0.0"
  }
}
